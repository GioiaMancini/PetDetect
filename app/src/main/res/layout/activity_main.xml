<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/background3"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"

    >

    <TextView
        android:layout_centerHorizontal="true"
        android:layout_width="300dp"
        android:layout_height="55dp"
        android:text="PetDetect"
        android:textStyle="bold"
        android:textColor="@color/success_button_color"
        android:id="@+id/title"
        android:textSize="40dp"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:layout_marginTop="20dp"
        android:background="@drawable/title_text_view_border_drawable"
        />

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_below="@+id/title"
        android:id="@+id/image_view"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Image"
        android:layout_below="@+id/image_view"
        android:id="@+id/select_button"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/success_button_background"
        android:textColor="#FDFDFD"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="8dp"
        android:drawableStart="@android:drawable/ic_menu_gallery"
        android:drawablePadding="8dp"
        android:elevation="2dp"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Capture Image"
        android:layout_below="@+id/select_button"
        android:id="@+id/capture_button"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/success_button_background"
        android:textColor="#FDFDFD"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="8dp"
        android:drawableStart="@android:drawable/ic_menu_camera"
        android:drawablePadding="8dp"
        android:elevation="2dp"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Detect Generic Object"
        android:layout_below="@+id/capture_button"
        android:id="@+id/predict_button"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/success_button_background"
        android:textColor="#FDFDFD"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="8dp"
        android:drawableStart="@android:drawable/ic_menu_view"
        android:drawablePadding="8dp"
        android:elevation="2dp"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Detect Pet"
        android:layout_below="@+id/predict_button"
        android:id="@+id/predict_cat_button"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/success_button_background"
        android:textColor="#FDFDFD"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="8dp"
        android:drawableStart="@drawable/paw_image_75x75"
        android:drawablePadding="8dp"
        android:elevation="2dp"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="50dp"
        android:text=""
        android:id="@+id/result_text"
        android:layout_below="@id/predict_cat_button"
        android:textSize="40dp"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:layout_marginTop="50dp"

        android:textColor="@color/success_button_color"
        android:background="@drawable/title_text_view_border_drawable"
        />
</RelativeLayout>